sudo: required
dist: trusty
addons:
  apt:
    packages:
    - python-subunit
    - rabbitmq-server
services:
  - rabbitmq
install:
    - sudo rabbitmq-plugins enable rabbitmq_management
    - sudo service rabbitmq-server restart
script: make check
